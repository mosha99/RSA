@page "/"
@using System.Text
@using RSA.Pages.Tools
@using RSA.Service


<CreateKeyPart @bind-GeneratedKeyValue="GeneratedKey"/>
<ShowKeyPart GeneratedKeys="GeneratedKey" />
<EncryptMessagePart PublicKey="@(GeneratedKey?.PublicKey)" ShareKey="@(GeneratedKey?.SharedKey)" />
<DecryptMessagePart PrivateKey="@(GeneratedKey?.PrivateKey)" ShareKey="@(GeneratedKey?.SharedKey)" />
 
@code
{
    GeneratedKeys? GeneratedKey { set; get; }
}
